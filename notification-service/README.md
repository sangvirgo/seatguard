# SeatGuard Notification Service

Kafka consumer + WebSocket gateway for real-time notifications.

## Run

```bash
npm install
npm run start:dev
```

Service runs on **port 3000**.

## Endpoints

- `GET /health` — health check
